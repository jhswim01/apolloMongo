type User {
  _id: ID!
  firstName: String!
  lastName: String!
  # adminProject: [Project]
  company: [Company]
  # issue: [Issue]
  # dailyLog: [DailyLog]
}

type Query {
  getUser(_id: String!): User
}
